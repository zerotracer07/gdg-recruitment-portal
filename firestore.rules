rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Deny all direct client access by default.
    // All reads/writes go through server-side API routes (firebase-admin).
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
